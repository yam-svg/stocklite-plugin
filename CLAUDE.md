# StockLite 插件开发说明

## 项目结构关键路径

| 文件                                       | 用途                                         |
|------------------------------------------|--------------------------------------------|
| `src/main/resources/META-INF/plugin.xml` | 插件版本号、description（功能介绍）、change-notes（更新日志） |
| `build.gradle.kts`                       | Gradle 版本号，必须与 plugin.xml 保持一致             |
| `README.md`                              | 项目说明文档，含功能列表和版本更新说明                        |
| `MARKETPLACE.md`                         | 上架流程和打包说明，含版本号引用                           |

---

## 指令：「更新插件说明」

当收到「更新插件说明」「更新插件信息」「准备发版」等指令时，执行以下操作：

### 第一步：确定新版本号

- 当前版本在 `plugin.xml` 第 4 行：`<version>X.Y.Z</version>`
- 版本规则：
  - 新增功能模块 → 次版本号 +1（如 1.8.0 → 1.9.0）
  - 仅 bug 修复/小优化 → 修订号 +1（如 1.8.0 → 1.8.1）
- **同步修改以下两处**，必须保持一致：
  1. `plugin.xml`：`<version>新版本号</version>`
  2. `build.gradle.kts`：`version = "新版本号"`

### 第二步：更新 plugin.xml

**`<description>` 字段**（功能介绍，中英双语）：
- 根据本次新增功能，在对应语言章节新增 `<h3>` 段落
- 已有章节有新能力时追加 `<li>` 条目
- 保持中英文对称，不删除已有功能描述

**`<change-notes>` 字段**（更新日志）：
- **清空所有历史版本记录**，只保留本次版本的更新内容
- 格式：
  ```xml
  <change-notes><![CDATA[
  <h3>X.Y.Z</h3>
  <h4>功能分类名</h4>
  <ul>
    <li>具体功能描述，说明做了什么、解决了什么问题</li>
  </ul>
  ]]></change-notes>
  ```
- 每项描述简要概括, 不列出技术细节, 只说明增加了什么功能或修复了什么问题即可
- 要确保代码中确实已存在该功能, 不要添加不存在的功能描述

### 第三步：更新 README.md

- **版本号**：更新文件顶部的 `**当前版本：X.Y.Z**`
- **功能列表**：新增功能在表格中加行，新增功能标注 `✅ X.Y 新增`
- **功能说明章节**：用 `## vX.Y.Z 新功能说明` 格式替换旧的功能说明，说明本次改动
- **项目结构**：如有新增文件，在结构表中补充

### 第四步：更新 MARKETPLACE.md

- 将文中所有旧版本号（如 `1.8.0`）替换为新版本号
- 用 `replace_all: true` 一次性替换

---

## 注意事项

- change-notes 只保留**本次版本**，历史记录全部删除
- description 中英文都要更新，不能只改一种语言
- 版本号必须在 plugin.xml 和 build.gradle.kts **两处同时修改**，否则构建产物文件名与 Marketplace 版本不一致
- 功能描述语气用「新增」「支持」「优化」「修复」，避免「改善了」「增强了」等模糊表达


# Claude 工作规范

## 工作原则

任何开发任务都必须遵循 **方案设计 → 实施开发 → 测试验证** 的流程，禁止直接开始编写代码。

---

# 第一阶段：需求分析

收到需求后，首先理解用户真实目标，而不是立即修改代码。

需要分析：

- 本次需求的目标是什么
- 会影响哪些模块
- 是否存在潜在风险
- 是否有更好的实现方式
- 是否与现有架构冲突

对于需求存在歧义时，应先提出问题确认，而不是自行假设。

---

# 第二阶段：输出实施方案

正式修改代码前，必须先输出实施方案。

方案至少包含：

## 一、需求理解

简要描述用户需求。

## 二、修改范围

列出将要修改的文件，例如：

- src/pages/order/index.tsx
- src/components/Table/index.tsx
- src/services/order.ts

## 三、实现思路

说明准备如何实现，包括：

- 数据流调整
- 状态管理变化
- API修改
- UI修改
- 新增组件
- 删除逻辑

必要时可以给出流程图或伪代码。

## 四、风险分析

说明可能带来的影响，例如：

- 是否影响已有功能
- 是否存在兼容性问题
- 是否影响性能
- 是否影响类型定义

只有方案确认后，再开始编码。

---

# 第三阶段：实施开发

开发过程中遵循以下原则：

## 优先复用

优先：

- 复用已有组件
- 复用已有Hook
- 复用已有工具函数

避免重复实现。

---

## 控制修改范围

仅修改完成需求所必须的代码。

不要顺手重构大量无关代码。

---

# 第四阶段：代码检查

完成后自行检查：

## 功能检查

确认：

- 是否实现需求
- 是否遗漏边界情况
- 是否存在空值问题
- 是否存在异常流程

---

## 类型检查

确认：

- TypeScript无错误
- 类型完整
- 无any滥用

---

## 代码质量检查

确认：

- 无重复代码
- 无无用变量
- 无调试代码
- 无console.log
- 无TODO遗留

---

# 第五阶段：测试验证

完成开发后，必须进行测试分析。

输出测试报告。

格式如下：

## 测试内容

列出测试场景，例如：

- ✅ 正常流程
- ✅ 空数据
- ✅ 边界数据
- ✅ 接口异常
- ✅ 重复点击
- ✅ 加载状态
- ✅ 权限控制
- ✅ 国际化
- ✅ 响应式布局（如适用）

---

## 测试结果

例如：

| 测试项 | 结果 |
| ------- | ---- |
| 正常流程 | ✅ 通过 |
| 空数据 | ✅ 通过 |
| 边界数据 | ✅ 通过 |
| 接口异常 | ✅ 通过 |

---

## 风险说明

指出仍然存在的问题，例如：

- 未连接真实环境验证
- 未进行压力测试
- 未覆盖极端并发场景
- 依赖后端接口正确返回

---

## 最终结论

明确给出最终结论，例如：

✅ 已完成需求开发。

本次修改已完成代码自检，并根据代码逻辑进行了测试分析。

测试结果显示：

- 功能实现符合需求
- 未发现明显逻辑问题
- 未发现类型错误
- 修改范围符合预期

如需进一步验证，建议在测试环境进行完整联调及回归测试。

---

# 输出规范

每次任务统一采用以下流程：

1. 理解需求
2. 输出实施方案
3. 等待确认（如用户未明确要求直接开发）
4. 实施开发
5. 自检代码
6. 输出测试报告
7. 给出最终结论

禁止跳过方案设计直接修改代码。

---

# 沟通原则

- 对需求保持批判性思考，发现潜在问题及时指出。
- 不确定的地方主动询问，不凭猜测实现。
- 对重大架构调整提前说明影响。
- 回答应简洁、准确、专业，避免冗余描述。
