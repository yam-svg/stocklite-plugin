# StockLite 插件开发说明

## 项目结构关键路径

| 文件                                       | 用途                                         |
|------------------------------------------|--------------------------------------------|
| `src/main/resources/META-INF/plugin.xml` | 插件版本号、description（功能介绍）、change-notes（更新日志） |
| `build.gradle.kts`                       | Gradle 版本号，必须与 plugin.xml 保持一致             |
| `README.md`                              | 项目说明文档，含功能列表和版本更新说明                        |
| `MARKETPLACE.md`                         | 上架流程和打包说明，含版本号引用                           |

---

## 指令：「更新插件说明」

当收到「更新插件说明」「更新插件信息」「准备发版」等指令时，执行以下操作：

### 第一步：确定新版本号

- 当前版本在 `plugin.xml` 第 4 行：`<version>X.Y.Z</version>`
- 版本规则：
  - 新增功能模块 → 次版本号 +1（如 1.8.0 → 1.9.0）
  - 仅 bug 修复/小优化 → 修订号 +1（如 1.8.0 → 1.8.1）
- **同步修改以下两处**，必须保持一致：
  1. `plugin.xml`：`<version>新版本号</version>`
  2. `build.gradle.kts`：`version = "新版本号"`

### 第二步：更新 plugin.xml

**`<description>` 字段**（功能介绍，中英双语）：
- 根据本次新增功能，在对应语言章节新增 `<h3>` 段落
- 已有章节有新能力时追加 `<li>` 条目
- 保持中英文对称，不删除已有功能描述

**`<change-notes>` 字段**（更新日志）：
- **清空所有历史版本记录**，只保留本次版本的更新内容
- 格式：
  ```xml
  <change-notes><![CDATA[
  <h3>X.Y.Z</h3>
  <h4>功能分类名</h4>
  <ul>
    <li>具体功能描述，说明做了什么、解决了什么问题</li>
  </ul>
  ]]></change-notes>
  ```
- 每项描述简要概括, 不列出技术细节, 只说明增加了什么功能或修复了什么问题即可
- 要确保代码中确实已存在该功能, 不要添加不存在的功能描述

### 第三步：更新 README.md

- **版本号**：更新文件顶部的 `**当前版本：X.Y.Z**`
- **功能列表**：新增功能在表格中加行，新增功能标注 `✅ X.Y 新增`
- **功能说明章节**：用 `## vX.Y.Z 新功能说明` 格式替换旧的功能说明，说明本次改动
- **项目结构**：如有新增文件，在结构表中补充

### 第四步：更新 MARKETPLACE.md

- 将文中所有旧版本号（如 `1.8.0`）替换为新版本号
- 用 `replace_all: true` 一次性替换

---

## 注意事项

- change-notes 只保留**本次版本**，历史记录全部删除
- description 中英文都要更新，不能只改一种语言
- 版本号必须在 plugin.xml 和 build.gradle.kts **两处同时修改**，否则构建产物文件名与 Marketplace 版本不一致
- 功能描述语气用「新增」「支持」「优化」「修复」，避免「改善了」「增强了」等模糊表达
